## What

<!-- 1-2 sentences. What does this PR do? -->

## Why

<!-- What problem does it solve? Link the related issue if applicable
     (Fixes #123). Random feature drops without an issue will be closed. -->

## How I tested

- [ ] `npm run typecheck` clean
- [ ] `npm run lint` clean (0 warnings)
- [ ] `npm test` passing (36+)
- [ ] `npm run test:e2e` passing (42+)
- [ ] Manually verified on a real device (mobile + desktop)

## Anything reviewers should look at?

<!-- Tricky bits, things you'd want a second pair of eyes on,
     trade-offs you considered, etc. -->

## Checklist

- [ ] Conventional commit messages (`feat:` / `fix:` / `chore:` / ...)
- [ ] No `any`, no `@ts-ignore`, no new lint warnings
- [ ] No mention of AI/Claude/GPT/generative tooling
- [ ] Stayed inside Sheet design tokens (no new colors / fonts)
- [ ] If product behavior changed, `PLAN.md` + `SHIP_CHECKLIST.md` reflect it
